// Pantalla de acceso (/login).
// Split-screen: panel de marca (izquierda) + formulario (derecha).
// Tabs Administrador / Operador. Responsable: Dev 1.
export default function LoginPage() {
  return null;
}
